const userModel = require("./user-model")
const bookModel = require("./book-model")

module.exports = {
    userModel,
    bookModel
}
// This file is used to export all the models in one place, so that we can import them easily in other files.